<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/secondary_shade_8">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        >

        <ImageView
            android:id="@+id/logoImage"
            android:layout_width="217dp"
            android:layout_height="100dp"
            android:layout_marginBottom="24dp"
            android:layout_marginLeft="10dp"
            android:contentDescription="@string/logo"
            android:src="@drawable/klafkakain" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Pilih Metode Klasifikasi"
            android:textAlignment="center"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="@color/primary_black"
            android:layout_marginBottom="12dp" />

        <!-- Tombol Klasifikasi Langsung -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnKlasifikasiLangsung"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Klasifikasi Langsung"
            android:textColor="@color/primary_white"
            app:icon="@drawable/ic_camera"
            app:iconPadding="8dp"
            app:iconGravity="textStart"
            app:cornerRadius="12dp"
            app:iconTint="@color/primary_white"
            app:backgroundTint="@color/secondary_shade_1"
            android:layout_marginBottom="16dp"
            android:layout_marginLeft="20dp"
            android:layout_marginRight="20dp"
            />

        <!-- Tombol Klasifikasi Skala Konveksi -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnKlasifikasiKonveksi"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Klasifikasi Skala Konveksi"
            android:textColor="@color/primary_white"
            app:icon="@drawable/ic_konveksi"
            app:iconPadding="8dp"
            app:iconGravity="textStart"
            app:cornerRadius="12dp"
            android:layout_marginRight="20dp"
            android:layout_marginLeft="20dp"
            app:iconTint="@color/primary_white"
            app:backgroundTint="@color/secondary_blue_2" />
    </LinearLayout>
</ScrollView>
